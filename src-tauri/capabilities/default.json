{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the main window use Tauri core APIs. The spike's own commands (connect/get_topics/get_messages) are app commands and need no extra permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
